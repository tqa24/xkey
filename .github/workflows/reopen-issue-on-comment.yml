name: Reopen Issue on New Comment

on:
  issue_comment:
    types: [created]

permissions:
  issues: write

jobs:
  reopen-if-closed:
    runs-on: ubuntu-latest
    # Only run when issue is closed and comment is not from a bot
    if: |
      github.event.issue.state == 'closed' &&
      github.event.comment.user.type != 'Bot'

    steps:
      - name: Reopen issue
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.issues.update({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              state: 'open'
            });

            console.log(`✅ Reopened issue #${context.issue.number}`);

      - name: Add comment explaining reopen
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: '🔄 Issue đã được tự động mở lại do có comment mới từ @' + context.payload.comment.user.login
            });

      - name: Summary
        run: |
          echo "✅ Issue #${{ github.event.issue.number }} đã được reopen" >> $GITHUB_STEP_SUMMARY
          echo "👤 Comment từ: @${{ github.event.comment.user.login }}" >> $GITHUB_STEP_SUMMARY
          echo "🔗 Issue URL: ${{ github.event.issue.html_url }}" >> $GITHUB_STEP_SUMMARY
